# 隐私与安全

本项目是单用户本地测试版，不是经过完整安全审计的公网服务。

## 数据边界

服务只绑定回环地址。个人配置含密钥，岗位与记录可能含完整简历和回答，默认位于 ~/.interview-sim/，不属于公开仓库。

准备档案 preparations/*.json 也含完整任务输入、简历版本、建议、原答引用和确认历史。确认经历表示使用者认可自己的描述，不等于现实事实经过独立核验。禁止把准备档案、报告阶段缓存或历史备份当作可公开的技术日志。

原始文件不长期保存；图片解析可能使用短暂临时文件。提取文字保存在岗位 / 会话中。录音片段与未提交草稿只在当前页面内存中。

AI 功能会将必要文字或音频发送到使用者配置的服务；本地网页不等于离线推理。使用者需了解模型提供商的数据政策与费用。

## 公开反馈与贡献

- 不提交 Key、令牌、真实简历、录音、原始报告、配置或备份。
- 截图与示例使用虚构或充分脱敏的数据。
- 密钥曾公开时，应在提供商侧撤销更换；删除字符串不等于撤销泄露。
- 漏洞细节或凭据泄露不要贴公开 Issue。优先使用仓库可用的私密漏洞报告渠道；若未启用，可先提交不含敏感细节的联系请求。

## 使用限制

不要改为 0.0.0.0 监听、建立公网隧道或多人共享实例。本版没有面向公网的认证、完整 CSRF 防护、配额隔离或多租户权限。

只运行一个进程写同一数据目录；内部线程锁不提供跨进程事务。未完成任务在服务重启后标中断，需明确重试；取消只能阻止后续处理，不能保证提供商已接收请求不计费。

本地文件权限不是加密。备份、云盘同步和有权限访问电脑的软件都可能读取文件。清理数据前核对目录，不要误删项目或其他应用文件。

macOS/Linux 使用 0700/0600；Windows 依赖用户目录继承的 ACL，chmod 不能提供同等隔离。请使用自己的非共享用户目录，勿把数据目录放在公共盘；跨平台自动测试不等于完整 Windows ACL 审计。

## 依赖安全

从 1.5 起最低 Python 3.11，推荐 3.12；不再支持会安装到旧依赖的 Python 3.9/3.10。按 requirements.txt 的固定版本与 SHA-256 安装；更新过程见 CONTRIBUTING.md。CI 扫描运行与开发锁定清单，出现告警应评估和修复，不能以“仅本地运行”直接忽略。无已知漏洞不代表没有未知风险。
