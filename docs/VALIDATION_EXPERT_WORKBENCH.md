# 岗位准备与专家协作：本地验收

日期：2026-09-15。范围：从公开基线 `e35f1637f818e26995b28a55907e42c974008374` 开发的未发布源码更新，包含岗位准备、纯实战、独立评分和面后素材闭环。未创建新 Release，不把历史 CI 当成本次提交的验证。

## P1–P5 基线验收

本机 macOS / ARM64、Python 3.12、Node.js；模型全部使用合成替身，浏览器为 Playwright Chromium。

| 检查 | 实际结果 |
| --- | --- |
| `python -m tests.run_tests` | 172 项通过；导入应用前隔离数据目录 |
| `node --test tests/test_audio_runtime.cjs tests/test_voice_runtime.cjs tests/test_report_runtime.cjs` | 10 项通过 |
| `node --check` 所有 frontend/*.js | 通过 |
| `python -m tests.run_browser` | 完整流程通过，console_errors=[] |
| `python -m pip check` | 无损坏依赖 |
| `python -m pip wheel --no-deps --no-build-isolation .` | 构建成功；新增后端模块/前端资源纳入包 |
| Skill quick_validate、`git diff --check` | 通过 |

浏览器覆盖岗位双入口、三类准备任务、版本对比/逐项采纳、首题/两次追问/跳题、仅结束零报告调用、报告刷新恢复、失败下一题明确恢复、单题重答刷新与终止、旧回调隔离、旧版报告、新版分数/Markdown/JSON/打印、面后提取→确认/修订/拒绝→新版本→下一次面试。检查1440和1024宽度，窄桌面逐题反馈可读且无横向溢出。

独立审查曾复现并修复：旧分支/多级事实确认冲突、取消后迟到结果补发请求、题纲错误绑定、回答保存后的重启窗口、旧报告覆盖新任务、纯空白引用、汇总失败后重答丢失首次评分缓存。修复均补回归并再次验证。

最终同步复测另发现报告导出按钮在页面恢复前短暂启用，以及浏览器测试误把空状态说明当作已完成报告。已补默认禁用回归与延迟恢复测试；现在仅真实报告就绪后才能导出或打印，测试等待实际报告标识及确切反馈数量。

## 同日追加：简历内容建议与折叠体验

在上述基线后修正了“诊断退化为标点润色”和建议列表过长的问题。本轮实际复验：

- `python -m tests.run_tests`：180 项通过（新增 8 项回归）；`node --test tests/test_audio_runtime.cjs tests/test_voice_runtime.cjs tests/test_report_runtime.cjs`：10 项通过。
- 完整 `python -m tests.run_browser`、所有前端 JS 语法及 `git diff --check` 通过，console_errors=[]。1440/1024 宽度的新诊断折叠和展开截图已人工检查，无横向溢出。
- 新回归覆盖：只提供措辞建议时新规则拒绝通过、有来源的内容建议与正式改写分离、伪造引用拒绝、仅空白差异的唯一原文恢复、歧义/文字差异阻断，以及经真实性确认后采纳的实际存储闭环、旧版本不变与重复请求复用。
- 旧规则诊断过期但历史保留，其他准备任务不因本次规则升级失效；读取不会偷偷再调用模型。新模型请求的 schema 明确要求内容建议。
- 浏览器实测默认收起、键盘 Enter 展开、重新渲染后键盘焦点和展开状态保留、展开/收起全部、采纳后新版本及后续面试闭环。

验证使用虚构岗位、简历与替身模型，不调用个人 API、不重新诊断用户材料。实际模型是否能提出高质量建议仍需本地体验；类型与引用校验不能替代语义核查。本轮未新增依赖，上述构建与 Skill 校验为 P1–P5 基线记录，不冒充追加改动的重新构建结果。

## 明确未验证

- 本次变更尚未执行远程 GitHub Actions；虽然保留 Windows/macOS/Linux 矩阵，不能声称本次 Windows/Linux 实机已通过。
- 未调用真实模型、个人配置、麦克风或扬声器，不以替身结果保证真实评分质量、模型费用、网络时延或音色。
- 未重新生成或发布 Archify 图、未操作 BossHunter PR、未发布 GitHub、未改动运行中的用户服务。
- 隐私检查是变更文件/包内容的有限检查，不是全面安全审计；个人数据与秘密仍应由使用者保护。

## 后续验收与回退

先在合成测试数据下运行以上命令，再由使用者用自己的 API 在本地网页核对真实建议质量和语音体验。Windows 按 [兼容性清单](COMPATIBILITY.md)独立验证。发布前还需审查目标提交及同一提交的 CI、更新版本说明，并决定是否更新演示和结构图。

升级或回退前先结束所有面试/报告，停止旧服务，再启动对应代码；不要让两个进程写同一数据目录。回退代码不删除新增准备档案、报告或原始会话。旧版程序未必理解新报告，因此优先用本版本导出记录后再回退。
